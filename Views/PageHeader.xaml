<UserControl x:Class="MonolithVpnClient.Views.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextHi}"/>
        <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="13" Foreground="{StaticResource TextLow}" Margin="0,4,0,0"
                   Visibility="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToVis}}"/>
    </StackPanel>
</UserControl>
